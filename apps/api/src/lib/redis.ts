
// import { Redis } from 'ioredis';

// const redis:Redis = new Redis();
// redis.on("connect", () => { console.log('connect Redis Server successfully'); })
// redis.on("error", (error) => { console.log('connect Redis Server error', error); })


// export default redis